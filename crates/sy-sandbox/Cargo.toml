[package]
name = "sy-sandbox"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "SecureYeoman sandbox — seccomp-bpf, cgroup v2, Landlock enforcement"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
