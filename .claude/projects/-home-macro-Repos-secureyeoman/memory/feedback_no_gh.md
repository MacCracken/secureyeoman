---
name: No gh CLI
description: User does not use the GitHub CLI (gh) — never invoke it
type: feedback
---

Do not use the `gh` CLI tool. The user does not have it installed and does not want it used.

**Why:** User has corrected this multiple times — it's not available in their environment.

**How to apply:** For GitHub operations (releases, PRs, issues), use the web interface, git commands, or ask the user. Never run `gh` commands.
