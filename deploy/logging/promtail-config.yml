server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: secureyeoman-logs
    static_configs:
      - targets:
          - localhost
        labels:
          job: secureyeoman
          __path__: /var/log/secureyeoman/*.log

    pipeline_stages:
      - json:
          expressions:
            level: level
            event: event
            message: msg
            timestamp: time
      - labels:
          level:
          event:
      - timestamp:
          source: timestamp
          format: RFC3339Nano

  - job_name: secureyeoman-audit
    static_configs:
      - targets:
          - localhost
        labels:
          job: secureyeoman-audit
          __path__: /var/log/secureyeoman/audit/*.jsonl

    pipeline_stages:
      - json:
          expressions:
            level: level
            event: event
      - labels:
          level:
          event:
