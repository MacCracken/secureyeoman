apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "friday.fullname" . }}-test-connection"
  namespace: {{ include "friday.namespace" . }}
  labels:
    {{- include "friday.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox:1.36
      command: ["sh", "-c"]
      args:
        - |
          echo "Testing core health endpoint..."
          wget -qO- http://{{ include "friday.fullname" . }}-core:{{ .Values.core.port }}/health
          echo ""
          echo "Core health check passed!"
