apiVersion: v1
kind: Service
metadata:
  name: {{ include "secureyeoman.fullname" . }}-core
  namespace: {{ include "secureyeoman.namespace" . }}
  labels:
    {{- include "secureyeoman.componentLabels" (dict "context" . "component" "core") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.core.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secureyeoman.selectorLabels" (dict "context" . "component" "core") | nindent 4 }}
