global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alert-rules.yml"

scrape_configs:
  - job_name: "friday"
    static_configs:
      - targets: ["host.docker.internal:18789"]
    metrics_path: /metrics
    scrape_interval: 10s
