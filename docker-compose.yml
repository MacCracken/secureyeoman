services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: secureyeoman
      POSTGRES_USER: secureyeoman
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-secureyeoman_dev}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U secureyeoman"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    restart: unless-stopped

  core:
    build: .
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "18789:18789"
    volumes:
      - secureyeoman-data:/app/data
    env_file: .env
    environment:
      SECUREYEOMAN_HOST: "0.0.0.0"
      NODE_ENV: production
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-secureyeoman_dev}
      DATABASE_HOST: postgres
      GITHUB_OAUTH_CLIENT_ID: ${GITHUB_OAUTH_CLIENT_ID:-}
      GITHUB_OAUTH_CLIENT_SECRET: ${GITHUB_OAUTH_CLIENT_SECRET:-}
      GOOGLE_OAUTH_CLIENT_ID: ${GOOGLE_OAUTH_CLIENT_ID:-}
      GOOGLE_OAUTH_CLIENT_SECRET: ${GOOGLE_OAUTH_CLIENT_SECRET:-}
      GMAIL_OAUTH_CLIENT_ID: ${GMAIL_OAUTH_CLIENT_ID:-}
      GMAIL_OAUTH_CLIENT_SECRET: ${GMAIL_OAUTH_CLIENT_SECRET:-}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:18789/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  mcp:
    build: .
    command: ["node", "packages/mcp/dist/cli.js"]
    ports:
      - "3001:3001"
    env_file: .env
    environment:
      MCP_ENABLED: "${MCP_ENABLED:-true}"
      MCP_PORT: "3001"
      MCP_HOST: "0.0.0.0"
      MCP_TRANSPORT: "${MCP_TRANSPORT:-streamable-http}"
      MCP_AUTO_REGISTER: "${MCP_AUTO_REGISTER:-true}"
      MCP_CORE_URL: "http://core:18789"
      MCP_EXPOSE_FILESYSTEM: "${MCP_EXPOSE_FILESYSTEM:-false}"
      MCP_ALLOWED_PATHS: "${MCP_ALLOWED_PATHS:-}"
      # MCP_EXPOSE_WEB: "${MCP_EXPOSE_WEB:-false}"
      # MCP_ALLOWED_URLS: "${MCP_ALLOWED_URLS:-}"
      # MCP_WEB_RATE_LIMIT: "${MCP_WEB_RATE_LIMIT:-10}"
      # MCP_EXPOSE_WEB_SCRAPING: "${MCP_EXPOSE_WEB_SCRAPING:-true}"
      # MCP_EXPOSE_WEB_SEARCH: "${MCP_EXPOSE_WEB_SEARCH:-true}"
      # MCP_WEB_SEARCH_PROVIDER: "${MCP_WEB_SEARCH_PROVIDER:-duckduckgo}"
      # MCP_WEB_SEARCH_API_KEY: "${MCP_WEB_SEARCH_API_KEY:-}"
      # MCP_EXPOSE_BROWSER: "${MCP_EXPOSE_BROWSER:-false}"
      # MCP_BROWSER_ENGINE: "${MCP_BROWSER_ENGINE:-playwright}"
      # MCP_BROWSER_HEADLESS: "${MCP_BROWSER_HEADLESS:-true}"
      # MCP_BROWSER_MAX_PAGES: "${MCP_BROWSER_MAX_PAGES:-3}"
      # MCP_BROWSER_TIMEOUT_MS: "${MCP_BROWSER_TIMEOUT_MS:-30000}"
      MCP_RATE_LIMIT_PER_TOOL: "${MCP_RATE_LIMIT_PER_TOOL:-30}"
      MCP_LOG_LEVEL: "${MCP_LOG_LEVEL:-info}"
      NODE_ENV: production
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:3001/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    depends_on:
      core:
        condition: service_healthy
    profiles:
      - mcp
      - full
    restart: unless-stopped

  dashboard:
    image: node:20-alpine
    working_dir: /app
    command: sh -c "npm install && npm run dev:dashboard"
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - dashboard-node-modules:/app/node_modules
    environment:
      VITE_GATEWAY_URL: "http://core:18789"
      VITE_HOST: "0.0.0.0"
    depends_on:
      core:
        condition: service_healthy

volumes:
  pgdata:
  secureyeoman-data:
  dashboard-node-modules:
