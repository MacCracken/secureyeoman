# SecureYeoman Environment Configuration
# Copy this file to .env and fill in the values.

# ──────────────────────────────────────────
# Security Keys (required — generate your own, minimum 32 characters each)
# ──────────────────────────────────────────
SECUREYEOMAN_SIGNING_KEY=change-me-to-a-random-32char-key
SECUREYEOMAN_TOKEN_SECRET=change-me-to-a-random-32ch-tokn
SECUREYEOMAN_ENCRYPTION_KEY=change-me-to-a-random-32ch-ekey
SECUREYEOMAN_ADMIN_PASSWORD=change-me-to-a-random-32ch-pass

# ──────────────────────────────────────────
# AI Provider (at least one required)
# ──────────────────────────────────────────
ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# GOOGLE_GENERATIVE_AI_API_KEY=
# OLLAMA_BASE_URL="http://localhost:11434"
# OPENCODE_API_KEY=
# LMSTUDIO_BASE_URL="http://localhost:1234/v1"
# LOCALAI_BASE_URL="http://localhost:8080/v1"
# DEEPSEEK_API_KEY=
# MISTRAL_API_KEY=

# ──────────────────────────────────────────
# Database (defaults match docker-compose.yml)
# ──────────────────────────────────────────
POSTGRES_PASSWORD=secureyeoman_dev
# DATABASE_HOST=localhost
# DATABASE_USER=secureyeoman
# DATABASE_NAME=secureyeoman

# Test Database (used by Vitest — create with: createdb -U secureyeoman secureyeoman_test)
# ──────────────────────────────────────────
# TEST_DB_NAME=secureyeoman_test
# TEST_DB_USER=secureyeoman
# TEST_DB_PASSWORD=secureyeoman_dev
# TEST_DB_HOST=localhost
# TEST_DB_PORT=5432

# ──────────────────────────────────────────
# Server (optional — defaults shown)
# ──────────────────────────────────────────
# SECUREYEOMAN_PORT=18789
# SECUREYEOMAN_HOST="0.0.0.0"
# SECUREYEOMAN_LOG_LEVEL="info"

# ──────────────────────────────────────────
# MCP Service (optional — enable with: docker compose --profile mcp up)
# No manual token needed — MCP self-mints a service JWT using SECUREYEOMAN_TOKEN_SECRET.
# ──────────────────────────────────────────
# MCP_ENABLED=true
# MCP_PORT=3001
# MCP_HOST="127.0.0.1"
# MCP_TRANSPORT="streamable-http"
# MCP_AUTO_REGISTER=true
# MCP_CORE_URL="http://127.0.0.1:18789"
# MCP_EXPOSE_FILESYSTEM=false
# MCP_ALLOWED_PATHS=
# MCP_RATE_LIMIT_PER_TOOL=30
# MCP_LOG_LEVEL="info"

# ──────────────────────────────────────────
# Proxy Integration (Phase 8.5 — opt-in anti-bot proxy rotation)
# ──────────────────────────────────────────
# MCP_PROXY_ENABLED=false
# MCP_PROXY_PROVIDERS="scrapingbee"
# MCP_PROXY_STRATEGY="round-robin"
# MCP_PROXY_DEFAULT_COUNTRY="US"
# MCP_PROXY_SCRAPINGBEE_KEY=""
# MCP_PROXY_SCRAPERAPI_KEY=""
# MCP_PROXY_BRIGHTDATA_URL=""
# MCP_PROXY_MAX_RETRIES=3
# MCP_PROXY_RETRY_BASE_DELAY_MS=1000

# ──────────────────────────────────────────
# OAuth Connections (optional — enables GitHub/Google login on Dashboard > Connections)
# ──────────────────────────────────────────
# GITHUB_OAUTH_CLIENT_ID=
# GITHUB_OAUTH_CLIENT_SECRET=
# GOOGLE_OAUTH_CLIENT_ID=
# GOOGLE_OAUTH_CLIENT_SECRET=
# GMAIL_OAUTH_CLIENT_ID=     # Can reuse GOOGLE_OAUTH_CLIENT_ID if same GCP project
# GMAIL_OAUTH_CLIENT_SECRET= # Can reuse GOOGLE_OAUTH_CLIENT_SECRET
# GOOGLE_CALENDAR_OAUTH_CLIENT_ID=     # Defaults to GOOGLE_OAUTH_CLIENT_ID
# GOOGLE_CALENDAR_OAUTH_CLIENT_SECRET= # Defaults to GOOGLE_OAUTH_CLIENT_SECRET
# GOOGLE_DRIVE_OAUTH_CLIENT_ID=        # Defaults to GOOGLE_OAUTH_CLIENT_ID
# GOOGLE_DRIVE_OAUTH_CLIENT_SECRET=    # Defaults to GOOGLE_OAUTH_CLIENT_SECRET

# ──────────────────────────────────────────
# Generic Email / IMAP/SMTP (optional — configured per-integration in dashboard)
# No global env vars needed. All settings (host, port, credentials) are stored
# per-integration. Common presets:
#   ProtonMail Bridge: IMAP 127.0.0.1:1143, SMTP 127.0.0.1:1025, self-signed certs
#   Outlook:           IMAP outlook.office365.com:993, SMTP smtp.office365.com:587
#   Yahoo:             IMAP imap.mail.yahoo.com:993, SMTP smtp.mail.yahoo.com:465
# Note: When running in Docker, use host.docker.internal instead of 127.0.0.1
# to reach services (e.g. ProtonMail Bridge) on the host machine.
# ──────────────────────────────────────────

# ──────────────────────────────────────────
# Developer Tools Integrations (optional — configured per-integration in dashboard)
# No global env vars needed. All settings (instance URL, tokens, keys) are stored
# per-integration in the database. Common integrations:
#   Jira:        instanceUrl + email + apiToken (Basic Auth)
#   AWS:         accessKeyId + secretAccessKey + region
#   Azure DevOps: organizationUrl + personalAccessToken + project
# ──────────────────────────────────────────

# ──────────────────────────────────────────
# Integration Plugins (optional — dynamic integration loading)
# Drop .mjs/.js plugin files in this directory to auto-load custom integration adapters.
# ──────────────────────────────────────────
# INTEGRATION_PLUGIN_DIR=/opt/secureyeoman/plugins

# ──────────────────────────────────────────
# Community Skills — path to a skills directory (relative to cwd or absolute)
# Default: ./community-skills (bundled in the project, works in Docker out of the box)
# To use the full community repo: clone https://github.com/MacCracken/secureyeoman-community-skills
# and set COMMUNITY_REPO_PATH to that clone's path.
# Trigger sync via: POST /api/v1/marketplace/community/sync  or  Dashboard > Skills > Community
# ──────────────────────────────────────────
# COMMUNITY_REPO_PATH=./community-skills

# ──────────────────────────────────────────
# Redis (optional — enables distributed rate limiting)
# ──────────────────────────────────────────
# REDIS_URL="redis://localhost:6379"
